import Display from './Display';
import GameInterface from './GameInterface';

const Game = () => {
    return (
        <section id="game">
            <Display />
            <GameInterface />
        </section>
    );
};

export default Game;
